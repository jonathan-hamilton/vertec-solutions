import { Container, Typography } from "@mui/material";

export default function Contact() {
  return (
    <Container>
      <Typography variant="h3" gutterBottom>
        Contact Page
      </Typography>
      <Typography>Let's connect — form coming soon!</Typography>
    </Container>
  );
}
